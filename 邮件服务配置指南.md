# 小说阅读平台 - 邮件服务配置指南

## 一、当前邮件服务限制

目前使用的 Resend 邮件服务有以下限制：
- 仅能发送到验证过的域名邮箱
- 无法发送到公共邮箱（outlook.com、gmail.com、qq.com 等）
- 免费额度有限

## 二、正式邮件服务方案推荐

### 方案一：腾讯云邮件推送（推荐）

**优势：**
- 国内访问速度快
- 支持中国大陆地区
- 性价比高
- 与腾讯云其他服务集成良好

**配置步骤：**

1. 注册腾讯云账号：https://cloud.tencent.com/

2. 开通邮件推送服务：
   - 访问 https://console.cloud.tencent.com/ses
   - 点击"立即开通"
   - 选择适合的套餐（新用户有免费额度）

3. 配置发送域名：
   - 在"域名管理"中添加您的域名
   - 按照提示配置 DNS 解析（MX、TXT 记录）
   - 等待域名验证通过

4. 创建发件人：
   - 在"发信人地址"中添加发件邮箱
   - 完成验证

5. 获取 API 密钥：
   - 在"API 密钥管理"中创建密钥
   - 保存 SecretId 和 SecretKey

6. 更新代码配置：

在 `/workspace/routes/auth.js` 中：

```javascript
// 替换 sendEmail 函数
async function sendEmail(to, subject, html) {
    const tencentcloud = require("tencentcloud-sdk-nodejs");
    const SesClient = tencentcloud.ses.v20201002.Client;

    const clientConfig = {
        credential: {
            secretId: process.env.TENCENT_SECRET_ID,
            secretKey: process.env.TENCENT_SECRET_KEY,
        },
        region: "ap-guangzhou",
        profile: {
            httpProfile: {
                endpoint: "ses.tencentcloudapi.com",
            },
        },
    };

    const client = new SesClient(clientConfig);
    const params = {
        FromEmailAddress: "noreply@yourdomain.com", // 替换为您的发件人
        Destination: [to],
        Subject: subject,
        Content: {
            Html: html,
        },
    };

    try {
        await client.SendEmail(params);
        console.log("✅ 邮件发送成功");
        return true;
    } catch (error) {
        console.error("❌ 邮件发送失败:", error);
        return false;
    }
}
```

7. 配置环境变量：

在 Railway 中添加：
```
TENCENT_SECRET_ID=your_secret_id
TENCENT_SECRET_KEY=your_secret_key
```

### 方案二：阿里云邮件推送

**优势：**
- 稳定可靠
- 国内服务支持
- 价格合理

**配置步骤类似腾讯云，详见：**
https://help.aliyun.com/product/29412.html

### 方案三：SendGrid（国际版）

**优势：**
- 全球知名服务
- API 简单易用
- 免费额度充足

**配置：**
```javascript
const sgMail = require('@sendgrid/mail');
sgMail.setApiKey(process.env.SENDGRID_API_KEY);

async function sendEmail(to, subject, html) {
    const msg = {
        to: to,
        from: 'noreply@yourdomain.com',
        subject: subject,
        html: html,
    };
    await sgMail.send(msg);
    return true;
}
```

### 方案四：Mailgun

**优势：**
- 开发者友好
- 完善的 API
- 追踪功能强大

**配置：**
```javascript
const mailgun = require('mailgun-js')({
    apiKey: process.env.MAILGUN_API_KEY,
    domain: process.env.MAILGUN_DOMAIN
});

async function sendEmail(to, subject, html) {
    const data = {
        from: 'noreply@yourdomain.com',
        to: to,
        subject: subject,
        html: html
    };
    await mailgun.messages().send(data);
    return true;
}
```

## 三、快速测试方案（临时）

如果您想快速测试，可以在代码中临时添加测试模式（仅限开发环境）：

```javascript
async function sendEmail(to, subject, html) {
    console.log('📧 模拟发送邮件:');
    console.log('   收件人:', to);
    console.log('   主题:', subject);
    console.log('   内容:', html.substring(0, 100) + '...');
    return true;
}
```

## 四、需要安装的依赖

根据选择的方案安装对应依赖：

```bash
# 腾讯云
npm install tencentcloud-sdk-nodejs

# SendGrid
npm install @sendgrid/mail

# Mailgun
npm install mailgun-js
```

## 五、常见问题

**Q: 为什么邮件发不出去？**
A: 请检查：
1. 域名是否验证通过
2. 发件人是否验证
3. API 密钥是否正确
4. 是否有额度限制

**Q: 邮件进了垃圾箱怎么办？**
A: 请配置 DKIM、SPF、DMARC 记录，提高邮件可信度。

**Q: 免费额度够吗？**
A: 一般新用户都有数千封/月的免费额度，初期足够使用。

---

如有问题，请联系技术支持。
